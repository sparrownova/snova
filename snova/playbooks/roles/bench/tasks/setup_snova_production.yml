---
- name: Setup production
  become: yes
  become_user: root
  command: snova setup production {{ frappe_user }} --yes
  args:
    chdir: '{{ snova_path }}'

- name: Setup Sudoers
  become: yes
  become_user: root
  command: snova setup sudoers {{ frappe_user }}
  args:
    chdir: '{{ snova_path }}'

- name: Set correct permissions on snova.log
  file:
    path: '{{ snova_path }}/logs/snova.log'
    owner: '{{ frappe_user }}'
    group: '{{ frappe_user }}'
  become: yes
  become_user: root

- name: Restart the snova
  command: snova restart
  args:
    chdir: '{{ snova_path }}'
...